TODO:

tirar flag islogged in quando LOGOUT DEVOLVE NOK

VER SE A ORDEM DE VERIFICAÇÃO ESTÁ CERTA 

    if (is_event_closed(event_EID)) {
        state = CLOSED;
    } else if (is_event_past(event_EID)) {
        state = PAST;
    } else if (is_event_sold_out(event_EID)) {
        state = SOLD_OUT;
    } else {
        state = ACCEPTING;
    }

USER:
confirmar que os read_tcp_field estao a fazer != SUCCESS ou == ERROR
-> muita diferenca por causa do EOM


TODO:

    - RESERVE RESERVATIONS POR EOM checking 
    - CTRL C SERVER-SIDE 
    - REVER STATUS TODOS DO SERVER ✅
    - UNREGISTER E LOGOUT 
    - SERVER CHAMAR-SE ES (compilado) 
    - PASS SER 8 DIGITOS EXATAMENTE (read field or error nao vê (ver reserve seats)) ✅
    - DOUBLE CHECK SE EID É SEMPRE PADDED (1 -> 001) ✅
    - O QUE É QUE SE PASSA COM VERIFICAR O TEMPO NO CREATE PQP
    - WRONG PASSWORD RECEIVED quer dizer que temos que dar logged out

DOUBT:
